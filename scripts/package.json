{
  "name": "saintdurbin-distribution",
  "version": "1.0.0",
  "description": "SaintDurbin yield distribution cron job with automatic validator switching",
  "main": "distribute.js",
  "scripts": {
    "distribute": "node distribute.js",
    "test": "node distribute.js --dry-run",
    "check-validator": "node check-validator.js"
  },
  "dependencies": {
    "ethers": "^6.9.0",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}